Now in V.1.1.0, in the latest version


to run it, you just must to find "PixelArtMaker" file in the folder ( Executable file jar )

So, you didn't required vscode again to run it, just find the Executable file jar that named "PixelArtMaker", and then just press it, simple!~
